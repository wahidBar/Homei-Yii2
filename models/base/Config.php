<?php
// This class was automatically generated by a giiant build task
// You should not change it manually as it will be overwritten on next build
// Modified by Defri Indra
// 2021

namespace app\models\base;

use Yii;

/**
 * This is the base-model class for table "t_config".
 *
 * @property integer $id
 * @property string $name
 * @property string $type
 * @property string $attribute
 * @property string $value_parser
 * @property string $value
 * @property integer $is_active
 * @property string $aliasModel
 */
abstract class Config extends \yii\db\ActiveRecord
{



    const SCENARIO_CREATE = 'create';
    const SCENARIO_UPDATE = 'update';
    public $_render = [];

    /**
     * @inheiritance
     */
    public function fields()
    {
        $parent = parent::fields();

        if (isset($parent['id'])) :
            unset($parent['id']);
            $parent['id'] = function ($model) {
                return $model->id;
            };
        endif;
        if (isset($parent['name'])) :
            unset($parent['name']);
            $parent['name'] = function ($model) {
                return $model->name;
            };
        endif;
        if (isset($parent['type'])) :
            unset($parent['type']);
        // $parent['type'] = function ($model) {
        //     return $model->type;
        // };
        endif;
        if (isset($parent['attribute'])) :
            unset($parent['attribute']);
        // $parent['attribute'] = function ($model) {
        //     return $model->attribute;
        // };
        endif;
        if (isset($parent['value_parser'])) :
            unset($parent['value_parser']);
        // $parent['value_parser'] = function ($model) {
        //     return $model->value_parser;
        // };
        endif;
        if (isset($parent['value'])) :
            unset($parent['value']);
            $parent['value'] = function ($model) {
                $parser = $model->value_parser;
                if ($parser) {
                    return $parser($model->value);
                }
                return $model->value;
            };
        endif;
        if (isset($parent['is_active'])) :
            unset($parent['is_active']);
        // $parent['is_active'] = function ($model) {
        //     return $model->is_active;
        // };
        endif;



        return $parent;
    }

    /**
     * @inheritdoc
     */
    public static function tableName()
    {
        return 't_config';
    }

    /**
     * @inheritdoc
     */
    public function rules()
    {
        return [
            [['name', 'type'], 'required'],
            [['attribute', 'value'], 'string'],
            [['is_active'], 'integer'],
            [['name'], 'string', 'max' => 100],
            [['type'], 'string', 'max' => 75]
        ];
    }

    /**
     * @inheritdoc
     */
    public function attributeLabels()
    {
        return [
            'id' => Yii::t('cruds', 'ID'),
            'name' => Yii::t('cruds', 'Nama'),
            'type' => Yii::t('cruds', 'Tipe'),
            'attribute' => Yii::t('cruds', 'Attribute'),
            'value_parser' => Yii::t('cruds', 'Value Parser'),
            'value' => Yii::t('cruds', 'Nilai'),
            'is_active' => Yii::t('cruds', 'Tampilkan'),
        ];
    }

    /**
     * @inheritdoc
     */
    public function attributeHints()
    {
        return array_merge(parent::attributeHints(), [
            'type' => Yii::t('cruds', 'Tipe dari inputan'),
            'attribute' => Yii::t('cruds', 'attribute dari tipe inputan'),
            'value_parser' => Yii::t('cruds', 'Mengubah tipe data dari nilai'),
        ]);
    }



    /**
     * @inheritdoc
     * @return \app\models\query\ConfigQuery the active query used by this AR class.
     */
    public static function find()
    {
        return new \app\models\query\ConfigQuery(get_called_class());
    }



    public function scenarios()
    {
        $parent = parent::scenarios();

        $columns = [
            'id',
            'name',
            'type',
            'attribute',
            'value_parser',
            'value',
            'is_active',
        ];

        $parent[static::SCENARIO_CREATE] = $columns;
        $parent[static::SCENARIO_UPDATE] = $columns;
        return $parent;
    }

    public function setRender($arr)
    {
        $this->_render = array_merge($this->_render, $arr);
    }

    public function removeRender($arr)
    {
        unset($this->_render[$arr]);
    }

    /**
     * Simplify return data xD
     */
    public function render()
    {
        return array_merge($this->_render, [
            "model" => $this,
        ]);
    }

    /**
     * override validate
     */
    public function validate($attributeNames = null, $clearErrors = true)
    {
        return parent::validate($attributeNames, $clearErrors);
    }

    /**
     * override load
     */
    public function load($data, $formName = null, $service = "web")
    {
        return parent::load($data, $formName);
    }
}
